# Backend Memory
> SpecWeave learnings - auto-pruned to 20 entries
> Updated: 2026-01-05

## Learnings
- L0105-e739 (H): That's correct - use the API endpoint. No, don't do that - use the other approach. 
- L0105-47a3 (M): That's correct - use the API endpoint. No, don't do that - use the other approach. 
- L0105-aa3e (H): That's correct - use the API endpoint. No, don't do that - use the other approach. 
- L0105-5dee (M): That's correct - use the API endpoint. No, don't do that - use the other approach. 
