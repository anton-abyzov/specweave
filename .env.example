# GitHub Personal Access Token (optional - gh CLI is preferred locally)
# Get from: https://github.com/settings/tokens
# Required scopes: repo, workflow
# Note: In CI/CD, GITHUB_TOKEN is auto-provided by GitHub Actions
GH_TOKEN=ghp_your_github_token_here

# Azure DevOps Personal Access Token
# Get from: https://dev.azure.com/{org}/_usersSettings/tokens
# Required scopes: Work Items (Read, Write, Manage), Code (Read), Project (Read)
AZURE_DEVOPS_PAT=your-ado-pat-52-chars-base64
AZURE_DEVOPS_ORG=your-organization-name
AZURE_DEVOPS_PROJECT=your-project-name

# Jira API Token
# Get from: https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_API_TOKEN=your-jira-api-token
JIRA_EMAIL=your-email@example.com
JIRA_DOMAIN=your-domain.atlassian.net

# Test Execution Mode (optional)
# Set to 'true' to run real API integration tests (requires credentials above)
# Set to 'false' or omit to run only stub tests (no credentials needed)
RUN_INTEGRATION_TESTS=false
