/**
 * ConfigManager - Load and save SpecWeave configuration
 *
 * @deprecated Import from './config/config-manager.js' instead.
 * This file is a re-export shim for backward compatibility (0188).
 */

export { ConfigManager, getConfigManager, readConfig, writeConfig } from './config/config-manager.js';
