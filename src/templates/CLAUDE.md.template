<!-- SECTION:header required -->
@AGENTS.md

# {PROJECT_NAME}: Claude Code notes

Everything shared by every tool is in AGENTS.md, imported above. Only Claude-specific notes belong here.

- Skills: `/sw:increment "title"`, `/sw:do`, `/sw:auto`, `/sw:review`, `/sw:done`, `/sw:handoff`. Without the plugin (a Claude Code Projects thread or another cloud session) the same skills load from `.claude/skills/`.
- The SessionStart hook prints a short pickup; run `specweave pickup` for the next task's acceptance criteria.
- Use plan mode before writing a spec for anything bigger than a small fix.
<!-- /SECTION -->

<!-- SECTION:jev when="jev" -->
Jev is enabled here; its rules are in the Jev section of AGENTS.md.
<!-- /SECTION -->

## Project notes

(Claude-only notes; anything every tool needs goes in AGENTS.md)
