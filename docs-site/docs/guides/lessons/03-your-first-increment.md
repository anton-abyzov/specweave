---
sidebar_position: 4
title: "Lesson 3: Your First Increment"
description: "Hands-on tutorial creating a complete feature from start to finish"
---

# Lesson 3: Your First Increment (Hands-On)

**Duration**: 60 minutes
**Prerequisites**: Lessons 1-2 completed, SpecWeave initialized
**Outcome**: Complete a full increment cycle with real code

---

## What We're Building

A **Dark Mode Toggle** feature — simple enough to complete in one session, complex enough to demonstrate SpecWeave's power.

**Final result**:
- Toggle switch in settings
- Theme persists across sessions
- System preference detection
- Full test coverage

---

## Step 1: Create the Increment

Open Claude Code in your project directory:

```bash
/specweave:increment "Add dark mode toggle to settings"
```

### What Happens Behind the Scenes

```
┌──────────────────────────────────────────────────────────────┐
│                    AGENT ORCHESTRATION                       │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  PM Agent activates...                                       │
│  ├── Analyzing "dark mode toggle" requirement                │
│  ├── Creating user stories                                   │
│  ├── Defining acceptance criteria                            │
│  └── Writing spec.md                                         │
│                                                              │
│  Architect Agent activates...                                │
│  ├── Designing component architecture                        │
│  ├── Choosing state management approach                      │
│  ├── Documenting technical decisions                         │
│  └── Writing plan.md                                         │
│                                                              │
│  Tech Lead Agent activates...                                │
│  ├── Breaking down into tasks                                │
│  ├── Embedding test plans                                    │
│  ├── Estimating effort                                       │
│  └── Writing tasks.md                                        │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### Expected Output

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CREATING INCREMENT: 0001-dark-mode-toggle
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PM Agent working...
  ✓ Identified 2 user stories
  ✓ Defined 6 acceptance criteria
  → spec.md created

Architect Agent working...
  ✓ Designed ThemeProvider architecture
  ✓ Documented 2 technical decisions
  → plan.md created

Tech Lead Agent working...
  ✓ Created 5 tasks
  ✓ Embedded test plans
  ✓ Estimated: 6 hours
  → tasks.md created

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
INCREMENT READY
Location: .specweave/increments/0001-dark-mode-toggle/
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Next: Review specs, then run /specweave:do
```

---

## Step 2: Review the Generated Specs

**Always review before implementing!**

### Review spec.md

```bash
cat .specweave/increments/0001-dark-mode-toggle/spec.md
```

**Expected content** (generated by PM Agent):

```markdown
---
increment: 0001-dark-mode-toggle
status: planning
created: 2025-11-25
---

# Dark Mode Toggle Feature

## Summary

Enable users to toggle between light and dark themes in the application
settings, with automatic persistence and system preference detection.

## User Stories

### US-001: Manual Theme Toggle

**As a** user with visual preferences,
**I want** to toggle between light and dark mode in settings,
**So that** I can choose my preferred viewing experience.

#### Acceptance Criteria

- **AC-US1-01**: Toggle switch visible in settings page
- **AC-US1-02**: Clicking toggle changes theme immediately
- **AC-US1-03**: Theme persists across page refreshes
- **AC-US1-04**: Theme persists across browser sessions

### US-002: System Preference Detection

**As a** user with OS-level dark mode,
**I want** the app to detect my system preference,
**So that** it matches my device settings automatically.

#### Acceptance Criteria

- **AC-US2-01**: System preference detected on first visit
- **AC-US2-02**: Manual toggle overrides system preference

## Non-Functional Requirements

- **NFR-001**: Theme switch must complete within 100ms
- **NFR-002**: No flash of wrong theme on page load
```

### Review plan.md

```bash
cat .specweave/increments/0001-dark-mode-toggle/plan.md
```

**Expected architecture**:

```markdown
# Implementation Plan: Dark Mode Toggle

## Architecture

### Component: ThemeProvider

**Pattern**: React Context + localStorage
**File**: `src/providers/ThemeProvider.tsx`

**State**:
```typescript
type Theme = 'light' | 'dark' | 'system';

interface ThemeContextValue {
  theme: Theme;
  resolvedTheme: 'light' | 'dark';
  setTheme: (theme: Theme) => void;
}
```

### Technical Decisions

**TD-001**: CSS Variables for theming
- Rationale: No JS required for color changes, instant switching
- Alternative: CSS-in-JS (rejected: runtime overhead)

**TD-002**: localStorage for persistence
- Rationale: Simple, synchronous, no backend needed
- Key: `theme-preference`
```

### Review tasks.md

```bash
cat .specweave/increments/0001-dark-mode-toggle/tasks.md
```

**Expected tasks**:

```markdown
# Tasks: Dark Mode Toggle

## Progress: 0/5 tasks (0%)

### T-001: Create ThemeProvider Context (P1)

**Effort**: 1.5h | **AC-IDs**: AC-US1-02, AC-US1-03, AC-US1-04

**Implementation**:
- [ ] Create `src/providers/ThemeProvider.tsx`
- [ ] Define Theme type and context
- [ ] Implement theme state with useState
- [ ] Add localStorage persistence
- [ ] Create useTheme hook

**Test Plan** (BDD):
- **Given** theme is 'light'
- **When** setTheme('dark') called
- **Then** theme changes to 'dark'

**Test Cases**:
- Unit: theme_toggle_lightToDark_changesTheme
- Unit: theme_persist_afterRefresh_maintainsTheme
- Coverage: >95%

**Status**: [ ] pending

---

### T-002: Implement CSS Variables (P1)

**Effort**: 1h | **AC-IDs**: AC-US1-02, NFR-001

**Implementation**:
- [ ] Create `src/styles/themes.css`
- [ ] Define light theme variables
- [ ] Define dark theme variables
- [ ] Add data-theme attribute handling

**Status**: [ ] pending

---

### T-003: Create ThemeToggle Component (P1)

**Effort**: 1h | **AC-IDs**: AC-US1-01, AC-US1-02

**Implementation**:
- [ ] Create `src/components/ThemeToggle.tsx`
- [ ] Add toggle switch UI
- [ ] Connect to ThemeProvider
- [ ] Add accessibility labels

**Test Cases**:
- Unit: toggle_click_changesTheme
- Unit: toggle_render_showsCurrentTheme
- Coverage: >90%

**Status**: [ ] pending

---

### T-004: Add System Preference Detection (P2)

**Effort**: 1h | **AC-IDs**: AC-US2-01, AC-US2-02

**Implementation**:
- [ ] Add matchMedia listener
- [ ] Detect prefers-color-scheme
- [ ] Set initial theme on first visit
- [ ] Handle system preference changes

**Test Cases**:
- Unit: systemPreference_dark_setsInitialDark
- Unit: manualOverride_afterSystem_preservesManual
- Coverage: >90%

**Status**: [ ] pending

---

### T-005: Prevent Flash of Incorrect Theme (P2)

**Effort**: 0.5h | **AC-IDs**: NFR-002

**Implementation**:
- [ ] Add inline script in head
- [ ] Set theme before React hydration
- [ ] Handle no-JS fallback

**Status**: [ ] pending
```

---

## Step 3: Make Adjustments (Optional)

If the generated specs aren't quite right, adjust them:

```
"The spec looks good, but add AC-US1-05: toggle should
show sun/moon icons indicating current theme"
```

Claude will update the spec.md and tasks.md accordingly.

---

## Step 4: Execute the Tasks

Start the implementation:

```bash
/specweave:do
```

### What Happens

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
EXECUTING INCREMENT: 0001-dark-mode-toggle
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

T-001: Create ThemeProvider Context
├── Creating src/providers/ThemeProvider.tsx
├── Implementing theme state and context
├── Adding localStorage persistence
├── Creating useTheme hook
├── Writing tests...
├── Running tests: ✓ 4/4 passing
└── ✓ Task complete (1.5h)

T-002: Implement CSS Variables
├── Creating src/styles/themes.css
├── Defining light theme variables
├── Defining dark theme variables
├── Adding data-theme handling
└── ✓ Task complete (45m)

T-003: Create ThemeToggle Component
├── Creating src/components/ThemeToggle.tsx
├── Implementing toggle UI
├── Connecting to ThemeProvider
├── Adding accessibility
├── Writing tests...
├── Running tests: ✓ 3/3 passing
└── ✓ Task complete (1h)

T-004: Add System Preference Detection
├── Adding matchMedia listener
├── Implementing initial detection
├── Writing tests...
├── Running tests: ✓ 2/2 passing
└── ✓ Task complete (1h)

T-005: Prevent Flash of Incorrect Theme
├── Adding inline script
├── Testing hydration...
└── ✓ Task complete (30m)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
EXECUTION COMPLETE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Tasks: 5/5 complete
Tests: 9/9 passing
Coverage: 94%

Next: Run /specweave:next to validate and close
```

---

## Step 5: Review the Generated Code

Let's look at what was created:

### ThemeProvider.tsx

```typescript
// src/providers/ThemeProvider.tsx

import React, { createContext, useContext, useEffect, useState } from 'react';

type Theme = 'light' | 'dark' | 'system';
type ResolvedTheme = 'light' | 'dark';

interface ThemeContextValue {
  theme: Theme;
  resolvedTheme: ResolvedTheme;
  setTheme: (theme: Theme) => void;
}

const ThemeContext = createContext<ThemeContextValue | undefined>(undefined);

const STORAGE_KEY = 'theme-preference';

function getSystemTheme(): ResolvedTheme {
  if (typeof window === 'undefined') return 'light';
  return window.matchMedia('(prefers-color-scheme: dark)').matches
    ? 'dark'
    : 'light';
}

function resolveTheme(theme: Theme): ResolvedTheme {
  if (theme === 'system') return getSystemTheme();
  return theme;
}

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  const [theme, setThemeState] = useState<Theme>(() => {
    if (typeof window === 'undefined') return 'system';
    return (localStorage.getItem(STORAGE_KEY) as Theme) || 'system';
  });

  const resolvedTheme = resolveTheme(theme);

  useEffect(() => {
    // Apply theme to document
    document.documentElement.setAttribute('data-theme', resolvedTheme);
  }, [resolvedTheme]);

  useEffect(() => {
    // Listen for system preference changes
    const mediaQuery = window.matchMedia('(prefers-color-scheme: dark)');
    const handler = () => {
      if (theme === 'system') {
        document.documentElement.setAttribute('data-theme', getSystemTheme());
      }
    };
    mediaQuery.addEventListener('change', handler);
    return () => mediaQuery.removeEventListener('change', handler);
  }, [theme]);

  const setTheme = (newTheme: Theme) => {
    setThemeState(newTheme);
    localStorage.setItem(STORAGE_KEY, newTheme);
  };

  return (
    <ThemeContext.Provider value={{ theme, resolvedTheme, setTheme }}>
      {children}
    </ThemeContext.Provider>
  );
}

export function useTheme() {
  const context = useContext(ThemeContext);
  if (!context) {
    throw new Error('useTheme must be used within ThemeProvider');
  }
  return context;
}
```

### ThemeToggle.tsx

```typescript
// src/components/ThemeToggle.tsx

import React from 'react';
import { useTheme } from '../providers/ThemeProvider';

export function ThemeToggle() {
  const { theme, setTheme, resolvedTheme } = useTheme();

  const handleToggle = () => {
    // Cycle: system → light → dark → system
    const nextTheme =
      theme === 'system' ? 'light' :
      theme === 'light' ? 'dark' : 'system';
    setTheme(nextTheme);
  };

  return (
    <button
      onClick={handleToggle}
      aria-label={`Current theme: ${theme}. Click to change.`}
      className="theme-toggle"
    >
      {resolvedTheme === 'dark' ? (
        <MoonIcon aria-hidden="true" />
      ) : (
        <SunIcon aria-hidden="true" />
      )}
      <span className="sr-only">
        {theme === 'system' ? 'System' : theme}
      </span>
    </button>
  );
}

function SunIcon(props: React.SVGProps<SVGSVGElement>) {
  return (
    <svg viewBox="0 0 24 24" fill="currentColor" {...props}>
      <path d="M12 2v2m0 16v2M4.93 4.93l1.41 1.41m11.32 11.32l1.41 1.41M2 12h2m16 0h2M6.34 17.66l-1.41 1.41M19.07 4.93l-1.41 1.41M12 6a6 6 0 100 12 6 6 0 000-12z" />
    </svg>
  );
}

function MoonIcon(props: React.SVGProps<SVGSVGElement>) {
  return (
    <svg viewBox="0 0 24 24" fill="currentColor" {...props}>
      <path d="M21 12.79A9 9 0 1111.21 3 7 7 0 0021 12.79z" />
    </svg>
  );
}
```

### themes.css

```css
/* src/styles/themes.css */

:root,
[data-theme='light'] {
  --color-bg-primary: #ffffff;
  --color-bg-secondary: #f5f5f5;
  --color-text-primary: #1a1a1a;
  --color-text-secondary: #666666;
  --color-border: #e0e0e0;
  --color-accent: #0066cc;
}

[data-theme='dark'] {
  --color-bg-primary: #1a1a1a;
  --color-bg-secondary: #2d2d2d;
  --color-text-primary: #ffffff;
  --color-text-secondary: #a0a0a0;
  --color-border: #404040;
  --color-accent: #66b3ff;
}

/* Apply variables */
body {
  background-color: var(--color-bg-primary);
  color: var(--color-text-primary);
  transition: background-color 0.2s ease, color 0.2s ease;
}
```

### Test File

```typescript
// tests/unit/providers/ThemeProvider.test.tsx

import { renderHook, act } from '@testing-library/react';
import { ThemeProvider, useTheme } from '../../../src/providers/ThemeProvider';

describe('ThemeProvider', () => {
  beforeEach(() => {
    localStorage.clear();
    document.documentElement.removeAttribute('data-theme');
  });

  it('defaults to system theme', () => {
    const { result } = renderHook(() => useTheme(), {
      wrapper: ThemeProvider,
    });

    expect(result.current.theme).toBe('system');
  });

  it('changes theme when setTheme called', () => {
    const { result } = renderHook(() => useTheme(), {
      wrapper: ThemeProvider,
    });

    act(() => {
      result.current.setTheme('dark');
    });

    expect(result.current.theme).toBe('dark');
    expect(result.current.resolvedTheme).toBe('dark');
  });

  it('persists theme to localStorage', () => {
    const { result } = renderHook(() => useTheme(), {
      wrapper: ThemeProvider,
    });

    act(() => {
      result.current.setTheme('dark');
    });

    expect(localStorage.getItem('theme-preference')).toBe('dark');
  });

  it('loads persisted theme on mount', () => {
    localStorage.setItem('theme-preference', 'dark');

    const { result } = renderHook(() => useTheme(), {
      wrapper: ThemeProvider,
    });

    expect(result.current.theme).toBe('dark');
  });

  it('applies data-theme attribute to document', () => {
    const { result } = renderHook(() => useTheme(), {
      wrapper: ThemeProvider,
    });

    act(() => {
      result.current.setTheme('dark');
    });

    expect(document.documentElement.getAttribute('data-theme')).toBe('dark');
  });
});
```

---

## Step 6: Check Progress

At any time, check your progress:

```bash
/specweave:progress
```

**Output**:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
INCREMENT: 0001-dark-mode-toggle
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Status: IN-PROGRESS

Tasks:
  [x] T-001: Create ThemeProvider Context (1.5h)
  [x] T-002: Implement CSS Variables (45m)
  [x] T-003: Create ThemeToggle Component (1h)
  [x] T-004: Add System Preference Detection (1h)
  [x] T-005: Prevent Flash of Incorrect Theme (30m)

Progress: 5/5 tasks (100%)
Time: 4h 45m actual / 6h estimated

Tests: 9/9 passing
Coverage: 94% (target: 80%) ✓

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## Step 7: Complete the Increment

Use the magic command:

```bash
/specweave:next
```

### The 3-Gate Validation

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PM VALIDATION: 0001-dark-mode-toggle
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

GATE 1: Tasks ✅
  Total: 5 tasks
  P1 Complete: 3/3 (100%)
  P2 Complete: 2/2 (100%)
  Blocked: 0

GATE 2: Tests ✅
  Suites: 3 passing
  Tests: 9 passing
  Coverage: 94% (target: 80%)

GATE 3: Documentation ✅
  spec.md: ACs updated
  tasks.md: All marked complete
  Living docs: Synced

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
RESULT: ✅ ALL GATES PASS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Auto-closing increment...
  ✓ Status: completed
  ✓ Completed date: 2025-11-25
  ✓ WIP freed: 1/2 → 0/2

🎉 Increment 0001-dark-mode-toggle CLOSED!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
POST-CLOSURE QUALITY ASSESSMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Overall Score: 91/100 (EXCELLENT)

Dimensions:
  Clarity:         95/100 ✓✓
  Testability:     92/100 ✓✓
  Completeness:    90/100 ✓✓
  Feasibility:     95/100 ✓✓
  Maintainability: 88/100 ✓
  Edge Cases:      85/100 ✓
  Risk Assessment: 90/100 ✓✓

Quality Gate: ✅ PASS

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
NEXT WORK SUGGESTIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

No planned increments found.
Current WIP: 0/2 (both slots available)

Ready for new work:
  /specweave:increment "Your next feature"
```

---

## Step 8: Verify the Results

### Check the Increment Folder

```bash
ls -la .specweave/increments/0001-dark-mode-toggle/
```

**Expected**:
```
├── spec.md           # Business requirements ✓
├── plan.md           # Technical design ✓
├── tasks.md          # Implementation (all checked) ✓
├── metadata.json     # Status: completed
└── reports/
    └── qa-post-closure.md  # Quality report
```

### Check Living Docs

```bash
cat .specweave/docs/public/FEATURES.md
```

**The dark mode feature is now documented!**

### Check Code Files

```bash
ls src/providers/
# ThemeProvider.tsx ✓

ls src/components/
# ThemeToggle.tsx ✓

ls src/styles/
# themes.css ✓

npm test
# All tests passing ✓
```

---

## The Complete Workflow Summary

```
┌──────────────────────────────────────────────────────────────┐
│              YOU JUST COMPLETED A FULL CYCLE!                │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  1. /specweave:increment "dark mode"                         │
│     → spec.md, plan.md, tasks.md generated                   │
│                                                              │
│  2. Reviewed specs (optional adjustments)                    │
│     → Ensured requirements correct                           │
│                                                              │
│  3. /specweave:do                                            │
│     → Code generated, tests written                          │
│                                                              │
│  4. /specweave:progress                                      │
│     → Monitored completion                                   │
│                                                              │
│  5. /specweave:next                                          │
│     → Validated quality gates                                │
│     → Auto-closed increment                                  │
│     → Quality assessed                                       │
│     → Ready for next feature                                 │
│                                                              │
│  RESULT:                                                     │
│  ✓ Working code                                              │
│  ✓ Full test coverage                                        │
│  ✓ Permanent documentation                                   │
│  ✓ Knowledge preserved forever                               │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## Practice Exercise

**Challenge**: Create your own increment!

Choose one:
1. "Add user avatar upload to profile"
2. "Implement keyboard shortcuts"
3. "Add notification system"
4. Your own idea

**Steps**:
```bash
/specweave:increment "Your feature"
# Review the generated specs
/specweave:do
# Watch the implementation
/specweave:next
# Complete and close
```

**Success criteria**:
- [ ] Increment created and closed
- [ ] All tests passing
- [ ] Specs preserved in `.specweave/increments/`
- [ ] Living docs updated

---

## Common Issues & Solutions

### Issue: "Tests failing after generation"

```bash
# Check test output
npm test -- --verbose

# If imports wrong, regenerate:
"Fix the import paths in ThemeProvider.test.tsx"
```

### Issue: "Spec doesn't match my needs"

```
"Update spec.md: add AC-US1-05 for animated transition"
# Claude updates spec and tasks
```

### Issue: "Want to add a task"

```
"Add T-006 to tasks.md: Write E2E tests for theme toggle"
# Claude adds task and you can run /specweave:do
```

---

## What's Next?

You've completed your first increment! You now know:
- ✅ How to create increments
- ✅ What the three files contain
- ✅ How to execute and monitor
- ✅ How to close with quality gates

**Next lesson**: Master the `:next` command — your workflow superpower.

:next → [Lesson 4: Mastering the :next Command](./04-the-next-command)
