@AGENTS.md

## Claude Code notes

- Changes to hook behaviour: rebuild, then run `/reload-plugins` or restart Claude Code to load them.
- Use a subagent for wide searches across `src/sync/` and `src/integrations/`; they are large.
